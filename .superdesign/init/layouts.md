# Shared Layouts

No app shell, nav, sidebar, header, or footer components exist yet.
Mekasa is an AI-SDLC scaffold: native clients will be Jetpack Compose (Android) and SwiftUI (iOS).
React/Tailwind files under `design/mockups/` are design artifacts only — not a running web app.

Intended mobile shell (from product brief / user flows):
- Bottom or tab navigation after onboarding: Dashboard, Inventory, Shopping List, Spending, Add Items, Settings
- Onboarding is a linear flow without the main shell
- Brand name **Mekasa** must be a hero-level signal on branded surfaces

No layout source files to dump. When Superdesign generates mockups, establish a shared mobile frame and extract it as a DraftComponent afterward.

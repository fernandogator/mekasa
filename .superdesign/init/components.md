# Shared UI Components

**Framework detection:** React (JSX mockup stubs only). No package.json frontend app yet.
**Meta-framework:** none
**Component library:** none (placeholders pending Superdesign)
**CSS approach:** intended Tailwind for mockups; tokens live in `design/design-system.md`

No shared UI primitives (Button, Input, Dialog, Card, etc.) exist yet.
The only React files are screen-level mockup stubs under `design/mockups/`.
Full stub sources are included below so Superdesign can see current ground truth (null renders).

---

## Dashboard
- Path: `design/mockups/Dashboard.jsx`
- Spec: UI-001
- Description: Home dashboard placeholder; returns null until Superdesign replaces it
- Props: none

```jsx
/**
 * Placeholder — Superdesign will replace this file.
 * Spec: UI-001 | Design Artifact: design/mockups/Dashboard.jsx
 * Not application code. Do not ship.
 */
export default function Dashboard() {
  return null;
}
```

---

## OnboardingStoreSelection
- Path: `design/mockups/OnboardingStoreSelection.jsx`
- Spec: UI-002
- Description: Onboarding store-selection placeholder; returns null
- Props: none

```jsx
/**
 * Placeholder — Superdesign will replace this file.
 * Spec: UI-002 | Design Artifact: design/mockups/OnboardingStoreSelection.jsx
 * Not application code. Do not ship.
 */
export default function OnboardingStoreSelection() {
  return null;
}
```

---

## ShoppingList
- Path: `design/mockups/ShoppingList.jsx`
- Spec: UI-003
- Description: Shopping list placeholder; returns null
- Props: none

```jsx
/**
 * Placeholder — Superdesign will replace this file.
 * Spec: UI-003 | Design Artifact: design/mockups/ShoppingList.jsx
 * Not application code. Do not ship.
 */
export default function ShoppingList() {
  return null;
}
```

---

## AddItems
- Path: `design/mockups/AddItems.jsx`
- Spec: UI-004
- Description: Add-items hub placeholder (barcode/receipt/voice/manual/trash); returns null
- Props: none

```jsx
/**
 * Placeholder — Superdesign will replace this file.
 * Spec: UI-004 | Design Artifact: design/mockups/AddItems.jsx
 * Not application code. Do not ship.
 */
export default function AddItems() {
  return null;
}
```

---

## TrashStationMode
- Path: `design/mockups/TrashStationMode.jsx`
- Spec: UI-005
- Description: Trash station mode placeholder; returns null
- Props: none

```jsx
/**
 * Placeholder — Superdesign will replace this file.
 * Spec: UI-005 | Design Artifact: design/mockups/TrashStationMode.jsx
 * Not application code. Do not ship.
 */
export default function TrashStationMode() {
  return null;
}
```
